# Port the orchestrator server listens on (default: 3456)
PORT=3456

# Allowed CORS origin (default: * allows all origins)
# For production, set this to the chrome-extension origin:
#   CORS_ORIGIN=chrome-extension://<your-extension-id>
CORS_ORIGIN=*
